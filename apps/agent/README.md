# Agent microservice

Generates fake sensor events and publishes them to RabbitMQ.

```bash
cp .env.example .env
npm install
npm run start:dev
```

See the root [README](../../README.md) for architecture and Docker Compose usage.
